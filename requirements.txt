# Runtime dependencies for the Streamlit app (streamlit_stuff.py).
# Kept deliberately small: this is what Streamlit Community Cloud installs on
# every deploy. The modeling/notebook stack lives in requirements-dev.txt.
#
# Python 3.11 or 3.12.

streamlit>=1.30
pandas>=2.2,<3
numpy>=1.26,<2.3
plotly>=5.18
prophet>=1.1

# imported by the app but not currently plotted with; drop these two if you
# also drop the matplotlib/seaborn imports at the top of the app.
matplotlib>=3.8
seaborn>=0.13
